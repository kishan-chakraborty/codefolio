---
import Header from '../../components/Header.astro';
import Sidebar from '../../components/Sidebar.astro';

// Use Vite eager glob and compute URLs
const modules = import.meta.glob('./*.md', { eager: true });
const items = Object.entries(modules)
  .map(([path, mod]) => {
    const m: any = mod as any;
    const slug = path.replace('./', '').replace(/\.(md|mdx)$/, '');
    return {
      url: `/blog/${slug}`,
      title: m?.frontmatter?.title ?? '',
      date: m?.frontmatter?.date,
    };
  })
  .sort((a, b) => {
    const da = new Date(a.date || 0).getTime();
    const db = new Date(b.date || 0).getTime();
    return db - da;
  });
---

<!DOCTYPE html>
<html>
  <Header title="Blog | Kishan Chakraborty" />
  <body>
    <div class="content">
      <Sidebar selectedItem="blog" />
      <nav class="main">
        <div class="container">
          <div class="back-nav-container">
            <a href="/" class="back-nav-button w-inline-block">
              <img src="/assets/back.svg" alt="" class="back-nav-img" loading="lazy" />
              <div>Back</div>
            </a>
          </div>
          <div class="content-container">
            <h1>Blog</h1>
            <section class="items-container">
              <div class="resouce-items">
                <div class="single-item">
                  <h3><a href="/blog/rl/">Reinforcement Learning Blog</a></h3>
                  <p>Let's understand RL intuitively.</p>
                </div>
                <div class="single-item">
                  <h3><a href="/blog/paper summary/">Let's Summarize</a></h3>
                  <p>Quick bites of some interesting paper</p>
                </div>
              </div>
            </section>
          </div>
        </div>
      </nav>
    </div>
  </body>
</html>
